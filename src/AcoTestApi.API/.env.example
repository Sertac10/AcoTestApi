# Thermal Printer Service Configuration
PORT=5000

# Optional API Security Token. If empty, authentication is disabled.
# If configured, mutating requests (POST /connect, POST /print/*, etc.) must include header:
# X-Api-Token: YOUR_TOKEN_HERE
API_TOKEN=aco-secret-token
